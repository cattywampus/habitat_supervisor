source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

group :test do
  gem "webmock"
end

# Specify your gem's dependencies in habitat_supervisor.gemspec
gemspec
